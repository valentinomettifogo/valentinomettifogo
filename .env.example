# Public: inlined at build time via $env/static/public.
# On Vercel set these in Production, Preview and Development.
PUBLIC_SUPABASE_URL=https://your-ref.supabase.co
PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# Secrets: read at runtime via $env/dynamic/private, rotatable without a rebuild.
SUPABASE_SERVICE_ROLE_KEY=sb_secret_xxx

# Shared Qlik webhook token. Generate a long one, e.g.:
#   node -e "console.log(require('crypto').randomBytes(36).toString('base64url'))"
# Tenants may carry their own (webhook_token_hash column); this is the fallback.
QLIK_WEBHOOK_TOKEN=

# Default Google Chat space. Tenants may override it (chat_webhook_url).
GOOGLE_CHAT_WEBHOOK_URL=

# Who gets into /alerts.
ALERTS_ALLOWED_DOMAIN=companydomain.com
ALERTS_ALLOWED_EMAILS=valentino@email.com
